# 形式逻辑基础（解字参考资料 01）

> 本文件为解字各步骤提供形式逻辑规则库。步骤文件引用本文件时，按本节编号查阅。

## 1. 命题与真值

- **命题**：能判断真假（真 / 假）的陈述句。疑问句、祈使句、感叹句本身不是命题（除非可还原为判断）。
- **判断类别**（对应第三步的形式化标注）：
  - 事实判断：描述现实（是 / 不是）——有客观真假；
  - 价值判断：评价好坏（该 / 不该、好 / 坏）——严格说无客观真假，可作为"评价标准"处理；
  - 定义判断：约定词义（所谓 X 就是 Y）——真假取决于约定，偷换定义是常见手法。
- **同义反复（tautology）**：无论现实如何都为真的命题（"单身汉是未婚男子"），信息量为零——对应第三步标签"同义反复废话"。

## 2. 命题联结词与真值表

| 联结词 | 记号 | 读法 | 何时为假 |
| --- | --- | --- | --- |
| 否定 | ¬P | 非 P | P 真时为假 |
| 合取 | P∧Q | P 且 Q | 任一为假 |
| 析取 | P∨Q | P 或 Q（相容或） | 二者皆假 |
| 蕴含 | P→Q | 如果 P 那么 Q | 仅 P 真 Q 假时为假 |
| 等值 | P↔Q | P 当且仅当 Q | 真假不一致时 |

**常用等值：**

- 逆否：P→Q ≡ ¬Q→¬P（蕴含的"逆否命题"与原命题等价）；
- 德摩根：¬(P∧Q) ≡ ¬P∨¬Q；¬(P∨Q) ≡ ¬P∧¬Q；
- 蕴含改写：P→Q ≡ ¬P∨Q。

**充分条件与必要条件（关键易错点）：**

- 如果 P 那么 Q：P 是 Q 的**充分条件**（有 P 必有 Q，但无 P 也可能有 Q）；
- 只有 P 才 Q：P 是 Q 的**必要条件**，形式化为 Q→P；
- 充分必要条件：P↔Q。
- 常见误用：把充分条件当必要条件（"考上大学就会成功"——考上既非成功的充分也非必要条件，这句话两个方向都不成立）。

## 3. 量化

- **全称命题**：所有 S 都是 P（∀）；
- **特称命题**：有些 S 是 P（∃）；
- **单称命题**：某个 S 是 P。
- **否定规则**：¬(所有 S 是 P) = 有些 S 不是 P；¬(有些 S 是 P) = 所有 S 都不是 P。
- **警惕量词漂移**：前提"有些"，结论"所有" → 以偏概全（第五步"夸大适用范围"）。"有些"在逻辑上通常指"至少有一个"，不等于"少数"，也不等于"多数"。

## 4. 演绎推理规则（有效形式）

| 规则 | 形式 | 名称 |
| --- | --- | --- |
| 肯定前件（MP） | P→Q，P ⊢ Q | 有效 |
| 否定后件（MT） | P→Q，¬Q ⊢ ¬P | 有效 |
| 假言三段论（HS） | P→Q，Q→R ⊢ P→R | 有效 |
| 选言三段论（DS） | P∨Q，¬P ⊢ Q | 有效 |
| 联言分解 | P∧Q ⊢ P；P∧Q ⊢ Q | 有效 |
| 联言合成 | P，Q ⊢ P∧Q | 有效 |
| 附加 | P ⊢ P∨Q | 有效 |
| 构造性二难 | P→R，Q→R，P∨Q ⊢ R | 有效 |
| 归谬法 | 假设 ¬C 推出矛盾 ⊢ C | 有效 |

**常见的无效形式（形式谬误，见参考资料 03 第五节）：**

- **否定前件**：P→Q，¬P ⊢ ¬Q ✗；
- **肯定后件**：P→Q，Q ⊢ P ✗。

## 5. 直言三段论

- **结构**：大前提（含大项 P）、小前提（含小项 S）、中项 M。
- **有效条件（规则法）：**
  1. 中项至少周延一次；
  2. 前提中不周延的项，结论中不得周延（大项不当周延、小项不当周延）；
  3. 两个否定前提推不出结论；前提一肯定一否定，结论只能否定；
  4. 两个特称前提推不出结论；前提有特称，结论只能是特称。
- **检验方法**：Venn 图 / 规则法逐条核对。
- 示例（有效）：所有 M 是 P，所有 S 是 M ⇒ 所有 S 是 P。
- 示例（无效）：所有 P 是 M，所有 S 是 M ⇒ 所有 S 是 P（中项不周延：M 在两个前提中都是谓项，均不周延）。

## 6. 归纳推理

- **枚举归纳**：从若干例子到一般结论。结论是概率性的；强度取决于：样本量、样本代表性、是否认真搜索过反例。
- **穆勒五法**（因果推断的对照逻辑）：
  1. 求同法：不同场合唯一共同因素 → 该因素为原因；
  2. 求异法：两场合仅差一个因素，结果不同 → 该因素为原因；
  3. 求同求异并用法：正反两组对照；
  4. 共变法：因素变化伴随结果变化 → 相关（未必因果）；
  5. 剩余法：已知原因的剩余部分归给新因素。
  - 使用前提：对照组设计合理、时间先后正确、无共同原因混淆。
- **类比推理**：A 与 B 相似，A 有 P ⇒ B 有 P。强度取决于相似性是否与 P 相关（表面相似不算）。
- **统计归纳**：样本 X% ⇒ 总体 X%。查：随机性、样本量、基数、偏差（幸存者偏差等）。
- **要点**：归纳只能"支持"不能"证明"；"证据支持" ≠ "逻辑必然"。

## 7. 演绎、归纳与论证质量

- **有效（valid）**：形式正确——前提真则结论必真；
- **可靠 / 健全（sound）**：有效 + 前提全真；
- **归纳强度（strong）**：前提对结论的支持度高（概率性）；
- 本工具判断"内部自洽"用的是有效性；判断"值不值得信"还要叠加前提真实性与证据强度（对应参考资料 02 的 RSA 三标准）。

## 8. 可证伪性（波普尔）

- 一个断言若"任何可能的观测都无法推翻它"，则**不可证伪**；
- 不可证伪 ≠ 假，而是"无法用现实检验"，讨论其真伪没有意义；
- 本工具对不可证伪内容照常拆结构、查推理，但标注"无法在现实层面检验"（第一步 1.4、第六步报告提示）。
